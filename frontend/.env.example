# ──────────────────────────────────────────────────────────────────
# Variables de entorno para el contenedor Docker del frontend
# ──────────────────────────────────────────────────────────────────

# URL base del backend (sin trailing slash)
# nginx hace proxy de /api → ${BACKEND_URL}/api/
# Railway:       https://dental-backend.up.railway.app
# AWS ECS:       http://backend-alb.us-east-1.elb.amazonaws.com
# DigitalOcean:  https://dental-api.tu-dominio.com
BACKEND_URL=http://localhost:8080

# Puerto en que escucha nginx dentro del contenedor
# Railway, Render y similares inyectan $PORT automáticamente.
# No necesitas cambiarlo manualmente en esas plataformas.
PORT=80
